{
  "name": "mule",
  "version": "1.0.0",
  "description": "Dynamic headless CMS – Express + Prisma + SQLite",
  "type": "module",
  "scripts": {
    "dev": "nodemon server.js",
    "start": "node server.js",
    "generate-schema": "node scripts/generate-schema.js",
    "generate-schema:migrate": "node scripts/generate-schema.js --migrate"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "express": "^4.21.0",
    "pug": "^3.0.2"
  },
  "devDependencies": {
    "eslint": "^10.4.1",
    "nodemon": "^3.0.0",
    "prisma": "^5.0.0"
  }
}
